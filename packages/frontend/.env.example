# Contributor/local Bun workflow only.
# Leave unset to use same-origin /api in Docker, preview, and production.
# Set this only when running Vite directly against a separate backend origin.
# VITE_API_URL=http://localhost:3000
